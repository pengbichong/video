<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.pbc.video.dao.CommentMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="org.pbc.video.model.Comment">
        <id column="c_id" property="cId" />
        <result column="v_id" property="vId" />
        <result column="content" property="content" />
        <result column="from_uid" property="fromUid" />
        <result column="state" property="state" />
        <result column="date" property="date" />
        <result column="nickname" property="nickname" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        c_id, v_id, content, from_uid, state, date, nickname
    </sql>

</mapper>
